{
  "framework": null,
  "buildCommand": "pnpm --filter @workspace/notecanai run build && mkdir -p public && cp -r artifacts/notecanai/dist/. public",
  "outputDirectory": "public",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "build": {
    "env": {
      "BASE_PATH": "/",
      "PORT": "3000"
    }
  }
}
