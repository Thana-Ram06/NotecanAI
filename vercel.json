{
  "framework": null,
  "buildCommand": "pnpm --filter @workspace/notecanai run build",
  "outputDirectory": "artifacts/notecanai/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "build": {
    "env": {
      "BASE_PATH": "/",
      "PORT": "3000"
    }
  }
}
